#!/bin/sh

set -e

umask 077

basedir=~/.MCASH
dbfile="$basedir/DB_CONFIG"
cfgfile="$basedir/MCASH.conf"

[ -e "$basedir" ] || mkdir "$basedir"

# MCASH does not clean up DB log files by default
[ -e "$dbfile" ] || echo 'set_flags DB_LOG_AUTOREMOVE' > "$dbfile"

exec /usr/lib/MCASH/MCASH-qt "$@"
